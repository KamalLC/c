//22 fall 2b (except menu driven program

#include<conio.h>
#include<stdio.h>

void main(){
	float a = 3, b = 7, c = 5;
	
	float sm, sqr, mn;
	
	sm = a + b + c;
	sqr = a * a + b * b + c * c;
	mn = sm / 3;
	
	printf("addition = %f, sum of squares = %f, mean = %f", sm, sqr, mn); 
}
